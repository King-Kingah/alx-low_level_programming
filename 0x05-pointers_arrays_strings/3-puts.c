#include "holberton.h"

/**
 * main - prints a string, followed by a new line, to stdout.
 *
 * Description: Use _putchar only.
 */

void _puts(char *str)
{
int i;      
for (i = 0; s[i] != '\0'; ++i)
{
_putchar(str);
}
  _putchar('\n');
}
